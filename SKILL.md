---
name: trade-journal-simple
description: 维护 A 股交易记录 Excel 的登记助手（简单版）。触发场景：用户说"记录一笔交易""登记成交""更新交易记录""建仓""加仓""减仓""清仓""交易记录""帮我记一笔买入/卖出"或提供成交截图/口述要求登记交易时触发。本 skill 负责把每笔成交按固定字段写入 交易记录.xlsx，首次使用时自动生成 Excel 空模板与使用说明文档，并提示说明文档位置。只做交易登记，不做统计、盈亏分析或复盘。
---

# 交易记录登记助手（simple）

公众号：金方土2025

## 首次使用检测

处理任何登记请求前，先检查当前项目目录是否存在 `交易记录.xlsx`：

- **不存在** → 运行初始化脚本生成 Excel 空模板与说明文档：

  ```bash
  python scripts/init.py
  ```

  运行后明确回复用户：
  > 已在 `<项目路径>` 生成 `交易记录.xlsx` 和 `使用说明.md`，请先阅读 `使用说明.md` 了解字段填写规范。

- **已存在** → 跳过初始化，直接进入登记流程。

## 登记流程

处理每笔成交时，按以下顺序执行：

1. **识别可确认信息**：股票名称、日期、成交价格、成交金额等，只取截图/口述中可确认的部分。
2. **判断操作类型**：建仓 / 加仓 / 减仓 / 清仓。
3. **匹配交易编号（Trade ID）**：
   - 首次建仓 → 创建新的 Trade ID（如 T001、T002……）。
   - 同一只股票的后续加仓/减仓/清仓 → 优先匹配已有 Trade ID（按标的 + 操作连续性判断）。
4. **必填校验**：建仓必须记录「交易理由」和「止损价/风险控制」两列。止损比例/价位由用户自定，不预设任何固定比例。
5. **信息确认规则**：
   - 截图中无法确认的信息 → 不允许猜测，明确标记为「待确认」。
   - 缺少交易理由或止损 → 先向用户询问，补齐后才写入。
6. **写入**：全部信息确认完成后，再写入 `交易记录.xlsx` 的「交易记录」sheet（从第 2 行起逐笔新增）。
7. **不可覆盖**：历史成交记录只允许新增，不允许覆盖或修改。

## 字段定义

Excel「交易记录」sheet 共 10 列，表头与填写规范详见 `assets/使用说明.md`（首次运行时已复制到项目目录）。核心字段：

交易编号 / 日期 / 标的 / 操作 / 成交价 / 数量/金额 / 仓位比例 / 交易理由 / 止损价/风险控制 / 清仓复盘

操作类型固定为：建仓、加仓、减仓、清仓。

## 写入技术要点

- 用 openpyxl 操作 xlsx（读取现有内容，追加新行后保存），避免覆盖历史数据。
- 含英文逗号的字段（如"100股 / 12,345.00元"）写入时需正确处理，防止列错位。
- 写完后重新读取校验新增行是否完整、列数是否正确。

## 个性化提示

本 skill 的字段与规则为默认模板。完成后可提示用户：

> 以上为默认模板，建议按你自己的交易习惯个性化调整字段或规则；不会优化就直接把你的需求告诉你的 agent，让它帮你改。
